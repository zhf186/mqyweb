# Task 7 测试指南 - 工具栏组件

## 测试环境准备

### 启动应用
```bash
# 启动后端
cd backend/manqiyou-app
mvnw.cmd spring-boot:run

# 启动前端
cd frontend
npm run dev
```

### 访问地址
- 前端: http://localhost:3000
- 后端API: http://localhost:8080

## 测试步骤

### 1. 访问可视化编辑器

1. 打开浏览器访问 http://localhost:3000/admin/login
2. 使用管理员账号登录（如果需要）
3. 进入内容管理页面 `/admin/content`
4. 选择一个页面（如"home"）
5. 点击"预览"按钮
6. 应该跳转到 `/admin/visual-editor/home`

**预期结果**:
- ✅ 成功跳转到可视化编辑器页面
- ✅ 工具栏正确显示
- ✅ 页面在iframe中加载

### 2. 测试工具栏布局

#### 桌面端测试 (≥1024px)
1. 调整浏览器窗口到1920x1080
2. 观察工具栏布局

**预期结果**:
- ✅ 工具栏为单行布局
- ✅ 左侧：返回按钮 + 页面信息
- ✅ 中间：设备选择器 + 语言切换按钮
- ✅ 右侧：编辑/预览按钮 + 保存按钮 + 关闭按钮
- ✅ 所有元素对齐良好

#### 移动端测试 (<1024px)
1. 调整浏览器窗口到768px宽度
2. 观察工具栏布局

**预期结果**:
- ✅ 工具栏为双行布局
- ✅ 第一行：返回按钮 + 页面信息 + 操作按钮
- ✅ 第二行：设备选择器 + 语言切换按钮（居中）
- ✅ 按钮文字在小屏幕上隐藏，只显示图标

### 3. 测试编辑模式切换

#### 进入编辑模式
1. 点击"编辑"按钮（Edit3图标）
2. 观察变化

**预期结果**:
- ✅ 按钮变为"预览"（Eye图标）
- ✅ 按钮样式从outline变为default
- ✅ Toast提示"已进入编辑模式"
- ✅ 页面上出现可编辑元素高亮
- ✅ 过渡动画流畅

#### 退出编辑模式
1. 点击"预览"按钮
2. 观察变化

**预期结果**:
- ✅ 按钮变回"编辑"
- ✅ 按钮样式变回outline
- ✅ Toast提示"已退出编辑模式"
- ✅ 可编辑元素高亮消失
- ✅ 过渡动画流畅

#### 防止快速切换
1. 快速连续点击编辑/预览按钮多次
2. 观察行为

**预期结果**:
- ✅ 在过渡期间点击无效
- ✅ 不会出现状态混乱
- ✅ 动画完成后才能再次切换

### 4. 测试设备尺寸切换

1. 点击设备选择器中的不同选项
2. 观察预览区域变化

**测试场景**:
- 桌面 (Desktop) → 全宽显示
- 平板 (Tablet) → 768px宽度
- 手机 (Mobile) → 375px宽度

**预期结果**:
- ✅ 预览区域宽度正确调整
- ✅ Toast提示当前设备尺寸
- ✅ 切换流畅无闪烁
- ✅ 编辑模式状态保持

### 5. 测试语言切换

#### 切换到英文
1. 点击语言切换按钮（显示"中文"）
2. 观察变化

**预期结果**:
- ✅ 按钮文字变为"EN"
- ✅ Toast提示"English"
- ✅ iframe内容切换到英文
- ✅ 如果在编辑模式，可编辑元素刷新

#### 切换回中文
1. 再次点击语言切换按钮（显示"EN"）
2. 观察变化

**预期结果**:
- ✅ 按钮文字变为"中文"
- ✅ Toast提示"中文"
- ✅ iframe内容切换回中文
- ✅ 如果在编辑模式，可编辑元素刷新

### 6. 测试保存功能

#### 初始状态
1. 刚进入编辑器时观察保存按钮

**预期结果**:
- ✅ 保存按钮禁用（灰色）
- ✅ 没有"有未保存的修改"提示

#### 进行修改
1. 进入编辑模式
2. 点击一个文字元素
3. 修改内容并保存
4. 观察工具栏变化

**预期结果**:
- ✅ 保存按钮启用（可点击）
- ✅ 桌面端显示"有未保存的修改"提示（橙色文字）
- ✅ 修改保存后，提示消失
- ✅ 保存按钮再次禁用

#### 点击保存按钮
1. 有未保存修改时点击保存按钮
2. 观察行为

**预期结果**:
- ✅ Toast提示"保存中..."
- ✅ Toast提示"保存成功"
- ✅ 未保存标记清除
- ✅ 保存按钮禁用

### 7. 测试关闭功能

#### 无修改时关闭
1. 没有进行任何修改
2. 点击"关闭"按钮

**预期结果**:
- ✅ 直接返回到 `/admin/content` 页面
- ✅ 没有显示确认对话框

#### 有修改时关闭
1. 进入编辑模式并修改内容
2. 点击"关闭"按钮
3. 观察对话框

**预期结果**:
- ✅ 显示关闭确认对话框
- ✅ 对话框标题："确认关闭？"
- ✅ 对话框内容：警告有未保存的修改
- ✅ 显示"取消"和"放弃修改并关闭"按钮

#### 取消关闭
1. 在确认对话框中点击"取消"

**预期结果**:
- ✅ 对话框关闭
- ✅ 继续停留在编辑器页面
- ✅ 修改状态保持

#### 确认关闭
1. 在确认对话框中点击"放弃修改并关闭"

**预期结果**:
- ✅ 对话框关闭
- ✅ 返回到 `/admin/content` 页面
- ✅ 修改被放弃

#### 返回按钮
1. 点击左上角的"返回"按钮
2. 观察行为

**预期结果**:
- ✅ 行为与"关闭"按钮相同
- ✅ 有修改时显示确认对话框
- ✅ 无修改时直接返回

### 8. 测试响应式布局

#### 桌面端 (1920x1080)
**预期结果**:
- ✅ 单行工具栏
- ✅ 所有文字可见
- ✅ 设备选择器和语言切换在中间
- ✅ "有未保存的修改"提示可见

#### 平板端 (768x1024)
**预期结果**:
- ✅ 双行工具栏
- ✅ 设备选择器和语言切换在第二行
- ✅ 按钮文字部分隐藏
- ✅ 图标清晰可见

#### 手机端 (375x667)
**预期结果**:
- ✅ 双行工具栏
- ✅ 按钮只显示图标
- ✅ 页面信息文字截断
- ✅ 所有功能可用

### 9. 测试交互流程

#### 完整编辑流程
1. 进入可视化编辑器
2. 切换到编辑模式
3. 切换语言到英文
4. 切换设备尺寸到手机
5. 点击一个元素进行编辑
6. 保存修改
7. 切换回中文
8. 切换回桌面尺寸
9. 退出编辑模式
10. 关闭编辑器

**预期结果**:
- ✅ 所有操作流畅无卡顿
- ✅ 状态切换正确
- ✅ Toast提示及时准确
- ✅ 没有错误或警告

## 常见问题排查

### 问题1: 工具栏不显示
**检查**:
- VisualEditorToolbar组件是否正确导入
- Props是否正确传递
- 浏览器控制台是否有错误

### 问题2: 编辑模式切换无效
**检查**:
- toggleMode函数是否正确实现
- iframe bridge是否正常工作
- 浏览器控制台查看消息通信

### 问题3: 语言切换无效
**检查**:
- CHANGE_LOCALE消息类型是否定义
- iframe是否接收到消息
- 前端页面是否支持语言切换

### 问题4: 保存按钮一直禁用
**检查**:
- hasUnsavedChanges状态是否正确更新
- 编辑操作是否正确设置状态
- 保存后是否清除状态

### 问题5: 关闭确认对话框不显示
**检查**:
- CloseConfirmDialog组件是否正确导入
- @radix-ui/react-alert-dialog是否安装
- isCloseDialogOpen状态是否正确管理

## 性能检查

### 响应时间
- 模式切换: < 300ms
- 语言切换: < 500ms
- 设备尺寸切换: < 200ms
- 对话框打开: < 100ms

### 内存使用
- 长时间使用不应有内存泄漏
- 切换操作不应导致内存持续增长

### 动画流畅度
- 所有过渡动画应该流畅
- 60fps无卡顿

## 测试完成检查清单

- [ ] 工具栏在桌面端正确显示
- [ ] 工具栏在移动端正确显示
- [ ] 编辑模式切换正常
- [ ] 语言切换正常
- [ ] 设备尺寸切换正常
- [ ] 保存功能正常
- [ ] 关闭功能正常
- [ ] 关闭确认对话框正常
- [ ] 响应式布局正确
- [ ] 所有Toast提示正确
- [ ] 无TypeScript错误
- [ ] 无浏览器控制台错误
- [ ] 性能表现良好

---

**测试完成后**: 如果所有测试通过，可以继续进行Task 8的开发。
