# Visual Editor Task 6 - 图片编辑功能测试指南

## 快速测试步骤

### 1. 启动应用
```bash
# 启动后端 (在一个终端)
cd backend/manqiyou-app
mvnw.cmd spring-boot:run

# 启动前端 (在另一个终端)
cd frontend
npm run dev
```

### 2. 访问可视化编辑器
1. 打开浏览器访问: http://localhost:3000/admin/login
2. 登录 (使用验证码: 123456)
3. 进入"内容管理"页面
4. 选择"首页 (home)"
5. 点击"预览"按钮

### 3. 测试图片编辑功能

#### 测试1: 打开图片编辑弹窗
1. 点击工具栏的"编辑"按钮进入编辑模式
2. 等待可编辑元素加载完成
3. 将鼠标悬停在页面上的图片上
4. 应该看到蓝色边框和"Hero背景图"标签
5. 点击图片
6. **预期结果**: 打开图片编辑弹窗，显示当前图片预览

#### 测试2: 图片路径输入和预览
1. 在图片编辑弹窗中，修改图片路径
2. 尝试输入: `/brand_assets/page1_img1.jpeg`
3. **预期结果**: 
   - 显示"新图片预览"区域
   - 显示加载动画
   - 加载完成后显示新图片

#### 测试3: 图片保存和实时更新
1. 确认新图片预览正确显示
2. 点击"保存"按钮
3. **预期结果**:
   - 显示"保存成功"提示
   - 弹窗自动关闭
   - iframe中的图片立即更新为新图片
   - 图片更新时有淡入淡出动画效果

#### 测试4: 错误处理
1. 再次点击图片打开编辑弹窗
2. 输入一个不存在的图片路径: `/invalid/path.jpg`
3. **预期结果**:
   - 新图片预览区域显示"图片加载失败"
   - 保存按钮被禁用
   - 无法保存错误的图片路径

#### 测试5: 键盘快捷键
1. 打开图片编辑弹窗
2. 修改图片路径
3. 按 `Ctrl+S` (Windows) 或 `Cmd+S` (Mac)
4. **预期结果**: 保存并关闭弹窗
5. 再次打开弹窗
6. 按 `Esc` 键
7. **预期结果**: 取消并关闭弹窗

## 详细测试场景

### 场景1: 修改Hero背景图
**目标**: 测试首页Hero区域的背景图片编辑

**步骤**:
1. 进入编辑模式
2. 点击页面顶部的大背景图片
3. 在弹窗中修改图片路径为: `/brand_assets/page3_img1.jpeg`
4. 观察预览
5. 点击保存

**验证点**:
- ✅ 弹窗正确打开
- ✅ 当前图片显示正确
- ✅ 新图片预览正确
- ✅ 保存成功
- ✅ 页面实时更新

### 场景2: 测试图片格式验证
**目标**: 验证只接受有效的图片格式

**步骤**:
1. 打开图片编辑弹窗
2. 尝试输入非图片文件: `/test.txt`
3. 点击保存

**验证点**:
- ✅ 显示错误提示: "请输入有效的图片路径（支持 jpg, png, gif, webp, svg）"
- ✅ 无法保存

### 场景3: 测试加载状态
**目标**: 验证图片加载时的状态显示

**步骤**:
1. 打开图片编辑弹窗
2. 输入一个较大的图片路径
3. 观察加载过程

**验证点**:
- ✅ 显示加载动画（旋转的图标）
- ✅ 加载完成后动画消失
- ✅ 图片正确显示

### 场景4: 测试取消操作
**目标**: 验证取消操作不会保存修改

**步骤**:
1. 打开图片编辑弹窗
2. 修改图片路径
3. 点击"取消"按钮
4. 再次打开同一图片的编辑弹窗

**验证点**:
- ✅ 图片路径恢复为原始值
- ✅ 页面上的图片没有变化

### 场景5: 测试多个图片编辑
**目标**: 验证可以连续编辑多个图片

**步骤**:
1. 编辑并保存第一个图片
2. 立即点击另一个图片
3. 编辑并保存第二个图片

**验证点**:
- ✅ 两个图片都正确更新
- ✅ 没有状态混乱
- ✅ 每次都显示正确的当前图片

## 可能遇到的问题

### 问题1: 图片不显示
**原因**: 图片路径不正确或图片文件不存在
**解决**: 
- 确保图片路径以 `/` 开头
- 确保图片文件存在于 `frontend/public/` 目录下
- 检查浏览器控制台的错误信息

### 问题2: 保存后没有更新
**原因**: iframe消息通信失败
**解决**:
- 检查浏览器控制台是否有错误
- 确认 `UPDATE_IMAGE` 消息已发送
- 刷新页面重试

### 问题3: 弹窗不打开
**原因**: 元素没有正确标记为可编辑
**解决**:
- 检查图片元素是否有 `data-editable` 属性
- 检查 `data-editable-type="image"` 是否正确设置
- 查看控制台日志确认元素检测结果

## 浏览器控制台日志

### 正常流程的日志
```
Edit mode initialized with locale: zh
Received 15 editable elements
Element clicked: { id: "editable-0", type: "image", ... }
Image updated: hero.background.image /brand_assets/page3_img1.jpeg
```

### 错误情况的日志
```
Failed to load image: /invalid/path.jpg
Image element not found for fieldKey: xxx
```

## 性能检查

### 加载时间
- 图片编辑弹窗打开: < 300ms
- 图片预览加载: 取决于图片大小
- 保存操作: < 1秒
- iframe更新: < 500ms

### 内存使用
- 打开/关闭弹窗不应导致内存泄漏
- 多次编辑不应导致性能下降

## 测试完成清单

- [ ] 图片编辑弹窗正确打开
- [ ] 当前图片预览正确显示
- [ ] 图片路径输入正常工作
- [ ] 新图片实时预览正常
- [ ] 图片加载状态正确显示
- [ ] 图片加载错误正确处理
- [ ] 保存功能正常工作
- [ ] iframe实时更新正常
- [ ] 更新动画效果正常
- [ ] 键盘快捷键正常工作
- [ ] 错误提示正确显示
- [ ] 取消操作正常工作
- [ ] 多个图片连续编辑正常
- [ ] 没有TypeScript错误
- [ ] 没有控制台错误

## 下一步

测试完成后，可以继续实现任务7：
- 创建VisualEditorToolbar组件
- 实现编辑模式切换
- 添加语言切换功能
- 添加保存和关闭按钮

---

**测试状态**: 待测试
**预计测试时间**: 15-20分钟
