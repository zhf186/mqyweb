# Visual Editor Task 4 - Testing Guide
## 编辑覆盖层功能测试指南

## 前提条件

1. **启动后端服务**:
   ```bash
   cd backend/manqiyou-app
   mvnw.cmd spring-boot:run
   ```

2. **启动前端服务**:
   ```bash
   cd frontend
   npm run dev
   ```

3. **登录后台管理系统**:
   - 访问: http://localhost:3000/admin/login
   - 使用管理员账号登录

## 测试步骤

### 1. 进入可视化编辑器

1. 登录后台管理系统
2. 点击左侧菜单 "内容管理"
3. 选择一个页面（例如 "home"）
4. 点击 "预览" 按钮
5. **预期结果**: 
   - 跳转到 `/admin/visual-editor/home`
   - 显示可视化编辑器界面
   - iframe 中加载首页预览
   - 显示 "预览加载完成" 提示

### 2. 进入编辑模式

1. 在可视化编辑器页面
2. 点击工具栏的 "编辑" 按钮
3. **预期结果**:
   - 按钮文字变为 "预览"
   - 显示 "已进入编辑模式" 提示
   - 显示 "可编辑元素已加载" 提示（如果页面有可编辑元素）
   - 在预览页面上看到蓝色的可编辑元素边框

### 3. 测试元素悬停

1. 在编辑模式下
2. 将鼠标移动到页面上的可编辑元素上
3. **预期结果**:
   - 元素显示浅蓝色边框 (`border-blue-400`)
   - 元素显示浅蓝色半透明背景 (`bg-blue-400/5`)
   - 元素上方显示标签提示
   - 标签包含:
     - 类型图标（文字/图片）
     - 字段名称
     - 类型标签（"文字" 或 "图片"）
   - 边框有轻微的脉冲动画

### 4. 测试元素点击

1. 在编辑模式下
2. 点击一个可编辑元素
3. **预期结果**:
   - 元素显示深蓝色边框 (`border-blue-500`)
   - 元素显示深蓝色半透明背景 (`bg-blue-500/10`)
   - 元素左上角显示蓝色圆点指示器
   - 显示 "元素已选中" 提示
   - 提示包含元素名称和类型
   - 控制台输出元素详细信息

### 5. 测试多个元素交互

1. 在编辑模式下
2. 依次悬停和点击不同的元素
3. **预期结果**:
   - 每次悬停都正确高亮当前元素
   - 每次点击都正确选中当前元素
   - 之前选中的元素取消选中状态
   - 状态切换流畅无延迟

### 6. 测试退出编辑模式

1. 在编辑模式下
2. 点击工具栏的 "预览" 按钮
3. **预期结果**:
   - 按钮文字变为 "编辑"
   - 显示 "已退出编辑模式" 提示
   - 所有可编辑元素的高亮消失
   - 覆盖层完全隐藏
   - 页面恢复正常预览状态

### 7. 测试设备尺寸切换

1. 在编辑模式下
2. 点击设备尺寸选择器
3. 切换到不同尺寸（桌面/平板/手机）
4. **预期结果**:
   - iframe 尺寸正确调整
   - 可编辑元素位置跟随调整
   - 高亮框位置正确
   - 标签提示位置正确

### 8. 测试语言切换

1. 在编辑模式下
2. 点击语言切换按钮
3. **预期结果**:
   - 显示 "语言已切换" 提示
   - iframe 内容切换到对应语言
   - 可编辑元素内容更新

### 9. 测试键盘操作

1. 在编辑模式下
2. 使用 Tab 键导航到可编辑元素
3. 按 Enter 或 Space 键
4. **预期结果**:
   - 元素被选中
   - 显示选中状态
   - 显示 "元素已选中" 提示

### 10. 测试无可编辑元素情况

1. 访问一个没有 `data-editable` 属性的页面
2. 进入编辑模式
3. **预期结果**:
   - 显示 "未检测到可编辑元素" 提示
   - 提示框有蓝色虚线边框
   - 提示用户确保页面已加载完成

## 视觉检查清单

### 边框样式
- [ ] 悬停时: 浅蓝色边框 + 浅蓝色背景
- [ ] 选中时: 深蓝色边框 + 深蓝色背景
- [ ] 边框宽度: 2px
- [ ] 过渡动画: 200ms

### 标签提示
- [ ] 位置: 元素上方 7px
- [ ] 背景: 悬停时浅蓝，选中时深蓝
- [ ] 包含: 图标 + 名称 + 类型标签
- [ ] 文字: 白色，12px
- [ ] 圆角: 有
- [ ] 阴影: 有

### 选中指示器
- [ ] 位置: 左上角
- [ ] 样式: 蓝色圆点，白色边框
- [ ] 大小: 12px × 12px

### 悬停动画
- [ ] 脉冲效果: 有
- [ ] 动画流畅: 是

## 常见问题排查

### 问题 1: 看不到可编辑元素
**可能原因**:
- 前端页面没有添加 `data-editable` 属性
- iframe 没有正确加载
- 消息通信失败

**解决方法**:
1. 检查浏览器控制台是否有错误
2. 确认 iframe 已加载完成
3. 检查前端页面是否有 `data-editable` 属性

### 问题 2: 点击元素没有反应
**可能原因**:
- 覆盖层 z-index 问题
- 事件处理函数未正确绑定
- 元素位置计算错误

**解决方法**:
1. 检查浏览器控制台是否有错误
2. 使用开发者工具检查覆盖层是否正确渲染
3. 检查元素位置是否正确

### 问题 3: 标签提示显示不正确
**可能原因**:
- 元素位置计算错误
- CSS 样式冲突
- 标签内容缺失

**解决方法**:
1. 检查元素的 `rect` 属性
2. 检查 CSS 样式是否正确应用
3. 确认元素有 `label` 属性

## 浏览器控制台检查

### 正常日志输出
```
[IframeBridge] Message sent: INIT_EDIT_MODE
[IframeBridge] Message sent: REQUEST_EDITABLE_ELEMENTS
Received message from iframe: { type: 'EDITABLE_ELEMENTS_RESPONSE', payload: [...] }
Received 10 editable elements
Element clicked: { id: 'editable-0', fieldKey: 'hero.title', ... }
```

### 错误日志
如果看到以下错误，需要修复:
- `Cannot send message: iframe contentWindow not available` - iframe 未加载
- `No handler registered for message type` - 消息处理器未注册
- `Rejected message from different origin` - 跨域问题

## 性能检查

### 响应时间
- [ ] 进入编辑模式 < 300ms
- [ ] 元素悬停响应 < 100ms
- [ ] 元素点击响应 < 100ms
- [ ] 退出编辑模式 < 300ms

### 内存使用
- [ ] 无内存泄漏
- [ ] 切换模式后正确清理状态
- [ ] 长时间使用无性能下降

## 无障碍性检查

- [ ] 所有可编辑元素有 ARIA 标签
- [ ] 支持键盘导航（Tab 键）
- [ ] 支持键盘操作（Enter/Space）
- [ ] 屏幕阅读器可以识别元素

## 下一步测试

完成 Task 4 测试后，继续测试:
- **Task 5**: 文字编辑功能
- **Task 6**: 图片编辑功能
- **Task 7**: 工具栏组件

## 报告问题

如果发现问题，请提供:
1. 问题描述
2. 重现步骤
3. 预期结果 vs 实际结果
4. 浏览器控制台错误信息
5. 截图或录屏

---

**测试完成标准**: 所有测试步骤通过，无严重 bug，用户体验流畅
