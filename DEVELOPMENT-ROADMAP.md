# 开发路线图（P0 / P1 / P2）

## 当前基线

- 短期开发主线以 `frontend/` + `backend/manqiyou-app/` 为准。
- `backend/manqiyou-gateway`、`backend/manqiyou-user`、`backend/manqiyou-route` 等模块当前保留为架构预留，不作为下一阶段功能开发的默认落点。
- 本地数据库优先统一为 `MySQL + Redis`，使用 `docker-compose.mysql.yml`。
- 后端运行基线仍以 `JDK 17` 为准；仓库当前已通过升级 Lombok 兼容本机 `JDK 25` 编译与测试。

## P0：统一开发基线

### 目标

- 跑通本地前后端基础链路。
- 固化单体后端开发入口。
- 清理环境、文档、部署脚本中的版本漂移。

### 任务

- 明确 `backend/manqiyou-app` 是当前唯一后端开发目标。
- 将本地 `JAVA_HOME` 和 `PATH` 切换到 `JDK 17`。
- 本地启动 `MySQL + Redis`，避免同时混用 `H2 / MySQL / PostgreSQL`。
- 验证前端 `test`、`lint`、`build`。
- 验证后端 `compile`、`test`、启动流程。
- 修正文档和部署脚本中写死的旧 JAR 名称。
- 让工作区默认忽略本地运行产物，保持仓库干净。

### 完成标准

- `frontend` 可以执行 `npm run test:run`、`npm run build`。
- `backend/manqiyou-app` 在 `JDK 17` 下可以编译和测试。
- 部署脚本不再依赖固定版本号的 JAR 文件名。
- 本地开发说明与实际运行方式一致。

## P1：补齐后台认证闭环

### 目标

- 让 CMS 后台认证从“本地 token 存在即可”升级为完整会话链路。

### 任务

- 前端补齐 `refreshToken` 类型和持久化逻辑。
- 增加统一的 `401` 处理和自动退出逻辑。
- 启动后台时主动调用 `/api/admin/auth/me` 做会话校验。
- 明确权限模型：`super_admin` 与 `content_editor` 的前端可见能力。
- 为登录、刷新、退出、恢复会话补最小覆盖测试。

### 完成标准

- 后台刷新页面后可恢复会话。
- token 过期时能自动处理，不会出现假登录态。
- 前后端对登录响应字段定义保持一致。

## P2：补齐 CMS 主功能闭环

### 目标

- 让内容管理、素材管理、可视化编辑具备可交付状态。

### 任务

- 实现内容页中的真实发布流程。
- 完成后台顶部搜索能力。
- 完成预览弹窗中的真实站点跳转逻辑。
- 收敛可视化编辑器的页面路由映射配置。
- 将“读取时自动补种数据”的逻辑逐步迁移到显式初始化流程。

### 完成标准

- CMS 的内容编辑、预览、发布形成完整链路。
- 公开页面与 CMS 页面 slug 映射关系可维护、可追踪。
- 读接口不再隐式修改数据库初始化状态。

## 建议执行顺序

1. 先完成 `P0`，特别是 `JDK 17 + MySQL/Redis + manqiyou-app`。
2. 再做 `P1`，先把认证状态和权限边界稳定下来。
3. 最后进入 `P2`，补齐 CMS 功能和可维护性。
