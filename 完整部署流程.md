# 漫骑游项目 - 完整部署流程

## 🎯 当前状态

- ✅ 项目代码已完成
- ✅ 部署配置已创建
- ✅ 密码已配置
- ✅ Git 已安装
- ⏳ 准备推送代码到 GitHub
- ⏳ 准备部署到阿里云服务器

---

## 📋 完整流程（共 3 个阶段）

### 阶段一：推送代码到 GitHub（本地操作）
### 阶段二：连接到服务器（SSH 连接）
### 阶段三：部署项目（服务器操作）

---

## 🚀 阶段一：推送代码到 GitHub

### 步骤 1：在正确目录执行 Git 操作

打开 PowerShell，执行：

```powershell
# 1. 进入正确的项目目录
cd D:\mrcweb1

# 2. 确认目录正确（应该看到 frontend, backend, deploy 等文件夹）
ls

# 3. 初始化 Git 仓库
git init

# 4. 配置用户信息
git config --global user.name "zhf186"
git config --global user.email "your-email@example.com"

# 5. 添加远程仓库
git remote add origin git@github.com:zhf186/mqyweb.git

# 6. 添加所有文件
git add .

# 7. 查看将要提交的文件
git status

# 8. 提交代码
git commit -m "初始提交：漫骑游项目完整代码和部署配置"

# 9. 设置分支名为 main
git branch -M main

# 10. 推送到 GitHub
git push -u origin main
```

### 步骤 2：验证推送成功

```powershell
# 查看推送结果
# 应该看到类似输出：
# To github.com:zhf186/mqyweb.git
#  * [new branch]      main -> main
# Branch 'main' set up to track remote branch 'main' from 'origin'.
```

访问 GitHub 确认：https://github.com/zhf186/mqyweb

---

## 🌐 阶段二：连接到阿里云服务器

### 步骤 1：使用 SSH 连接服务器

```powershell
# 连接到服务器
ssh root@47.97.21.33

# 输入密码后，您会看到服务器的命令行提示符
```

### 步骤 2：检查服务器环境

```bash
# 检查 Docker
docker --version

# 检查 Docker Compose
docker-compose --version

# 检查 Git
git --version

# 检查 Nginx
nginx -v
```

如果有任何工具未安装，请先安装。

---

## 🚀 阶段三：在服务器上部署项目

### 步骤 1：配置 SSH 密钥（用于 Git）

```bash
# 1. 生成 SSH 密钥
ssh-keygen -t ed25519 -C "your-email@example.com"
# 按 Enter 使用默认路径
# 按 Enter 跳过密码

# 2. 查看公钥
cat ~/.ssh/id_ed25519.pub

# 3. 复制公钥内容
```

**在浏览器中**：
1. 访问：https://github.com/settings/keys
2. 点击 "New SSH key"
3. Title: "阿里云服务器"
4. Key: 粘贴刚才复制的公钥
5. 点击 "Add SSH key"

**测试连接**：
```bash
ssh -T git@github.com
# 应该显示：Hi zhf186! You've successfully authenticated...
```

### 步骤 2：克隆项目代码

```bash
# 1. 创建项目目录
sudo mkdir -p /var/www/manqiyou
sudo chown -R $USER:$USER /var/www/manqiyou

# 2. 克隆代码
cd /var/www/manqiyou
git clone git@github.com:zhf186/mqyweb.git .

# 3. 确认文件已下载
ls -la
# 应该看到：frontend, backend, deploy, .env.production 等
```

### 步骤 3：配置环境变量

```bash
# 1. 复制环境变量文件
cp .env.production .env

# 2. 查看环境变量（密码已配置好）
cat .env

# 环境变量已经包含强密码，无需修改
# 如果需要修改，使用：nano .env
```

### 步骤 4：运行部署脚本

```bash
# 1. 添加执行权限
chmod +x deploy/quick-deploy.sh

# 2. 运行部署脚本
bash deploy/quick-deploy.sh
```

**脚本会自动执行**：
- 检查环境依赖
- 停止旧容器
- 构建 Docker 镜像（需要 10-15 分钟）
- 启动所有容器
- 验证服务状态

**等待构建完成**，您会看到：
```
✓ 环境检查通过
✓ 项目目录已创建
✓ 代码已更新
✓ 环境变量配置已就绪
✓ 旧容器已停止
✓ Docker 容器已启动
✓ 服务启动等待完成
✓ 服务状态检查完成
```

### 步骤 5：配置 Nginx

```bash
# 1. 复制 Nginx 配置
sudo cp /var/www/manqiyou/deploy/nginx-manqiyou.conf /etc/nginx/sites-available/manqiyou

# 2. 创建软链接
sudo ln -s /etc/nginx/sites-available/manqiyou /etc/nginx/sites-enabled/

# 3. 测试 Nginx 配置
sudo nginx -t

# 4. 重载 Nginx
sudo systemctl reload nginx

# 5. 检查 Nginx 状态
sudo systemctl status nginx
```

### 步骤 6：配置 SSL 证书

```bash
# 1. 编辑 SSL 脚本，修改邮箱
sudo nano /var/www/manqiyou/deploy/setup-ssl.sh
# 找到：EMAIL="your-email@example.com"
# 改为：EMAIL="your-actual-email@example.com"
# 保存：Ctrl+X, Y, Enter

# 2. 添加执行权限
sudo chmod +x /var/www/manqiyou/deploy/setup-ssl.sh

# 3. 运行 SSL 配置脚本
sudo bash /var/www/manqiyou/deploy/setup-ssl.sh
```

**按照提示操作**：
- 确认邮箱地址
- 同意服务条款
- 等待证书获取完成

### 步骤 7：验证部署

```bash
# 1. 测试 HTTPS 访问
curl -I https://www.zjmqy.cc

# 2. 测试后端 API
curl https://www.zjmqy.cc/api/health

# 3. 查看容器状态
cd /var/www/manqiyou
docker-compose -f docker-compose.prod.yml ps

# 4. 查看日志
docker-compose -f docker-compose.prod.yml logs -f
# 按 Ctrl+C 退出日志查看
```

---

## ✅ 部署完成检查

### 在浏览器中测试

1. **访问网站**：https://www.zjmqy.cc
   - [ ] 页面正常加载
   - [ ] 浏览器显示安全锁图标
   - [ ] 图片正常显示

2. **测试所有页面**：
   - [ ] 首页：https://www.zjmqy.cc
   - [ ] 关于：https://www.zjmqy.cc/about
   - [ ] 路线：https://www.zjmqy.cc/routes
   - [ ] E-BIKE：https://www.zjmqy.cc/ebike
   - [ ] 社区：https://www.zjmqy.cc/community
   - [ ] 合作伙伴：https://www.zjmqy.cc/partners
   - [ ] 好物：https://www.zjmqy.cc/goods

3. **测试功能**：
   - [ ] 中英文切换正常
   - [ ] 移动端响应式正常
   - [ ] 动画效果正常
   - [ ] API 接口正常

---

## 🔧 常用管理命令

### 查看服务状态

```bash
cd /var/www/manqiyou

# 查看所有容器
docker-compose -f docker-compose.prod.yml ps

# 查看日志
docker-compose -f docker-compose.prod.yml logs -f

# 查看特定服务日志
docker-compose -f docker-compose.prod.yml logs -f frontend
docker-compose -f docker-compose.prod.yml logs -f backend
```

### 重启服务

```bash
cd /var/www/manqiyou

# 重启所有服务
docker-compose -f docker-compose.prod.yml restart

# 重启特定服务
docker-compose -f docker-compose.prod.yml restart frontend
docker-compose -f docker-compose.prod.yml restart backend
```

### 更新代码

```bash
cd /var/www/manqiyou

# 拉取最新代码
git pull origin main

# 重新构建并启动
docker-compose -f docker-compose.prod.yml up -d --build
```

### 查看 Nginx 日志

```bash
# 访问日志
sudo tail -f /var/log/nginx/manqiyou_access.log

# 错误日志
sudo tail -f /var/log/nginx/manqiyou_error.log
```

---

## 🐛 故障排查

### 问题 1：容器无法启动

```bash
# 查看详细日志
docker-compose -f docker-compose.prod.yml logs

# 检查端口占用
sudo netstat -tulpn | grep -E ':(3000|8080|5432|6379)'

# 重启 Docker
sudo systemctl restart docker
```

### 问题 2：Nginx 502 错误

```bash
# 检查后端是否运行
curl http://localhost:8080/api/health

# 查看 Nginx 错误日志
sudo tail -f /var/log/nginx/manqiyou_error.log

# 检查容器状态
docker-compose -f docker-compose.prod.yml ps
```

### 问题 3：SSL 证书问题

```bash
# 检查证书状态
sudo certbot certificates

# 重新获取证书
sudo certbot delete --cert-name www.zjmqy.cc
sudo bash /var/www/manqiyou/deploy/setup-ssl.sh
```

---

## 📋 完整命令速查表

### 本地操作（PowerShell）

```powershell
# 推送代码
cd D:\mrcweb1
git init
git add .
git commit -m "初始提交"
git branch -M main
git remote add origin git@github.com:zhf186/mqyweb.git
git push -u origin main

# 连接服务器
ssh root@47.97.21.33
```

### 服务器操作（Bash）

```bash
# 配置 SSH 密钥
ssh-keygen -t ed25519 -C "your-email@example.com"
cat ~/.ssh/id_ed25519.pub
ssh -T git@github.com

# 克隆代码
sudo mkdir -p /var/www/manqiyou
sudo chown -R $USER:$USER /var/www/manqiyou
git clone git@github.com:zhf186/mqyweb.git /var/www/manqiyou
cd /var/www/manqiyou

# 部署
cp .env.production .env
bash deploy/quick-deploy.sh

# 配置 Nginx
sudo cp deploy/nginx-manqiyou.conf /etc/nginx/sites-available/manqiyou
sudo ln -s /etc/nginx/sites-available/manqiyou /etc/nginx/sites-enabled/
sudo nginx -t && sudo systemctl reload nginx

# 配置 SSL
sudo nano deploy/setup-ssl.sh  # 修改邮箱
sudo bash deploy/setup-ssl.sh

# 验证
curl https://www.zjmqy.cc
docker-compose -f docker-compose.prod.yml ps
```

---

## 🎉 部署成功！

当所有检查项都通过后，您的网站就成功部署了！

**访问地址**：https://www.zjmqy.cc

**恭喜！漫骑游项目已成功部署到阿里云！** 🚀

---

## 📖 参考文档

- **详细部署指南**：`deploy/阿里云部署完整方案.md`
- **检查清单**：`deploy/DEPLOYMENT_CHECKLIST.md`
- **快速参考**：`deploy/README-ALIYUN.md`
- **项目总结**：`PROJECT_SUMMARY.md`

---

**流程版本**: 1.0
**最后更新**: 2025-01-29
**预计时间**: 40-50 分钟
