# 项目精简说明

## 🎯 精简目标

为服务器部署做准备，删除不必要的文件，减小项目体积，提高部署效率。

---

## 📋 可以删除的文件

### 1. 开发文档（本地保留，不需要部署）
这些文件仅用于本地开发参考，不需要上传到服务器：

```
- 漫骑游官方网站开发全案说明书.md
- 漫骑游介绍.pdf
- 漫骑游品牌手册.pdf
- 内容优化方案.md
- SKILL.md
- spec-interview.md
- ENHANCEMENTS.md
- DEVELOPMENT.md
```

### 2. 安装指南（已完成安装，不再需要）
```
- 安装Git指南.md
- 安装Git-快速命令.bat
```

### 3. 临时说明文档（已阅读，可删除）
```
- 开始部署-操作指南.md
- 密码配置说明.md
- 项目精简说明.md（本文件）
```

### 4. 密码备份文件（已保存到安全地方）
```
- PASSWORDS_BACKUP.txt
```
**⚠️ 重要**：删除前请确保已将密码保存到密码管理器！

### 5. Windows 批处理脚本（服务器不需要）
```
- check-env.bat
- setup-assets.bat
- start-all.bat
- start-backend.bat
- start-frontend.bat
```

### 6. 开发环境 Docker 配置（使用生产配置）
```
- docker-compose.yml
```
**保留**：`docker-compose.prod.yml`（生产环境使用）

### 7. 前端开发文档（可选删除）
```
- frontend/QUICK_OPTIMIZATION_GUIDE.md
- frontend/OPTIMIZATION_SUMMARY.md
- frontend/PERFORMANCE_OPTIMIZATION.md
```

---

## ✅ 必须保留的文件

### 核心配置文件
```
✅ .env.production          # 生产环境配置
✅ .gitignore              # Git 忽略规则
✅ docker-compose.prod.yml # 生产环境 Docker 配置
✅ README.md               # 项目说明
✅ package-lock.json       # 依赖锁定文件
```

### 部署相关文件
```
✅ deploy/                 # 部署脚本和配置
   ├── quick-deploy.sh
   ├── setup-ssl.sh
   ├── nginx-manqiyou.conf
   ├── 阿里云部署完整方案.md
   ├── DEPLOYMENT_CHECKLIST.md
   └── README-ALIYUN.md
```

### 项目文档（精简版）
```
✅ DEPLOYMENT_READY.md     # 部署就绪说明
✅ PROJECT_SUMMARY.md      # 项目总结
✅ DEPLOYMENT.md           # 部署文档
```

### 源代码
```
✅ frontend/               # 前端源码
✅ backend/                # 后端源码
✅ brand_assets/           # 品牌素材
```

---

## 🗑️ 删除文件列表

### 可以安全删除的文件（共 18 个）

```bash
# 开发文档（8 个）
漫骑游官方网站开发全案说明书.md
漫骑游介绍.pdf
漫骑游品牌手册.pdf
内容优化方案.md
SKILL.md
spec-interview.md
ENHANCEMENTS.md
DEVELOPMENT.md

# 安装指南（2 个）
安装Git指南.md
安装Git-快速命令.bat

# 临时说明（3 个）
开始部署-操作指南.md
密码配置说明.md
项目精简说明.md

# 密码备份（1 个）
PASSWORDS_BACKUP.txt

# Windows 脚本（5 个）
check-env.bat
setup-assets.bat
start-all.bat
start-backend.bat
start-frontend.bat

# 开发环境配置（1 个）
docker-compose.yml

# 前端文档（3 个 - 可选）
frontend/QUICK_OPTIMIZATION_GUIDE.md
frontend/OPTIMIZATION_SUMMARY.md
frontend/PERFORMANCE_OPTIMIZATION.md
```

---

## 📊 精简效果

### 精简前
- 文件数量：~100+ 个文件
- 项目大小：~500MB（包含 node_modules 和 target）

### 精简后
- 文件数量：~80 个文件
- 项目大小：~50MB（不包含 node_modules 和 target）
- 减少：~50MB 文档和脚本

### Git 仓库大小
- 精简前：~30MB
- 精简后：~10MB
- 减少：~20MB

---

## 🚀 执行精简

### 方式一：手动删除

1. 打开文件资源管理器
2. 进入项目目录
3. 按照上面的列表逐个删除文件

### 方式二：使用 PowerShell 脚本

创建并运行 `精简项目.ps1`：

```powershell
# 进入项目目录
cd D:\mrcweb1

# 删除开发文档
Remove-Item "漫骑游官方网站开发全案说明书.md" -ErrorAction SilentlyContinue
Remove-Item "漫骑游介绍.pdf" -ErrorAction SilentlyContinue
Remove-Item "漫骑游品牌手册.pdf" -ErrorAction SilentlyContinue
Remove-Item "内容优化方案.md" -ErrorAction SilentlyContinue
Remove-Item "SKILL.md" -ErrorAction SilentlyContinue
Remove-Item "spec-interview.md" -ErrorAction SilentlyContinue
Remove-Item "ENHANCEMENTS.md" -ErrorAction SilentlyContinue
Remove-Item "DEVELOPMENT.md" -ErrorAction SilentlyContinue

# 删除安装指南
Remove-Item "安装Git指南.md" -ErrorAction SilentlyContinue
Remove-Item "安装Git-快速命令.bat" -ErrorAction SilentlyContinue

# 删除临时说明
Remove-Item "开始部署-操作指南.md" -ErrorAction SilentlyContinue
Remove-Item "密码配置说明.md" -ErrorAction SilentlyContinue

# 删除密码备份（确保已保存！）
Remove-Item "PASSWORDS_BACKUP.txt" -ErrorAction SilentlyContinue

# 删除 Windows 脚本
Remove-Item "check-env.bat" -ErrorAction SilentlyContinue
Remove-Item "setup-assets.bat" -ErrorAction SilentlyContinue
Remove-Item "start-all.bat" -ErrorAction SilentlyContinue
Remove-Item "start-backend.bat" -ErrorAction SilentlyContinue
Remove-Item "start-frontend.bat" -ErrorAction SilentlyContinue

# 删除开发环境配置
Remove-Item "docker-compose.yml" -ErrorAction SilentlyContinue

# 删除前端文档（可选）
Remove-Item "frontend/QUICK_OPTIMIZATION_GUIDE.md" -ErrorAction SilentlyContinue
Remove-Item "frontend/OPTIMIZATION_SUMMARY.md" -ErrorAction SilentlyContinue
Remove-Item "frontend/PERFORMANCE_OPTIMIZATION.md" -ErrorAction SilentlyContinue

Write-Host "精简完成！" -ForegroundColor Green
```

---

## ⚠️ 重要提示

### 删除前必做

1. **备份密码**
   ```
   确保 PASSWORDS_BACKUP.txt 中的密码已保存到密码管理器
   ```

2. **确认 Git 状态**
   ```bash
   git status
   # 确保重要文件已提交
   ```

3. **创建备份**（可选）
   ```bash
   # 压缩整个项目作为备份
   Compress-Archive -Path D:\mrcweb1 -DestinationPath D:\mrcweb1_backup.zip
   ```

### 删除后检查

```bash
# 查看项目结构
tree /F

# 确认必要文件存在
ls .env.production
ls docker-compose.prod.yml
ls deploy/
```

---

## 📝 更新 .gitignore

精简后，更新 `.gitignore` 确保不必要的文件不会被提交：

```gitignore
# 已添加的规则
PASSWORDS_BACKUP.txt
*.backup.txt
*_backup.txt

# 开发文档
*.pdf
*说明书.md
*指南.md

# Windows 脚本
*.bat

# 临时文件
项目精简说明.md
```

---

## ✅ 精简完成后

### 提交更改

```bash
# 查看删除的文件
git status

# 添加所有更改
git add .

# 提交
git commit -m "精简项目文件，准备部署"

# 推送到 GitHub
git push origin main
```

### 验证精简效果

```bash
# 查看项目大小
du -sh .

# 查看文件数量
find . -type f | wc -l

# 查看 Git 仓库大小
git count-objects -vH
```

---

## 🎯 精简后的项目结构

```
manqiyou/
├── .env.production              # 生产环境配置
├── .gitignore                   # Git 忽略规则
├── docker-compose.prod.yml      # Docker 配置
├── README.md                    # 项目说明
├── DEPLOYMENT_READY.md          # 部署就绪
├── PROJECT_SUMMARY.md           # 项目总结
├── DEPLOYMENT.md                # 部署文档
├── package-lock.json            # 依赖锁定
│
├── deploy/                      # 部署脚本
│   ├── quick-deploy.sh
│   ├── setup-ssl.sh
│   ├── nginx-manqiyou.conf
│   ├── 阿里云部署完整方案.md
│   ├── DEPLOYMENT_CHECKLIST.md
│   └── README-ALIYUN.md
│
├── frontend/                    # 前端源码
│   ├── src/
│   ├── public/
│   ├── Dockerfile
│   └── package.json
│
├── backend/                     # 后端源码
│   ├── manqiyou-app/
│   │   ├── src/
│   │   ├── Dockerfile
│   │   └── pom.xml
│   └── ...
│
└── brand_assets/                # 品牌素材
    └── ...
```

---

## 🚀 下一步

精简完成后：

1. ✅ 提交更改到 Git
2. ✅ 推送到 GitHub
3. ✅ 开始服务器部署

**参考文档**：`deploy/阿里云部署完整方案.md`

---

**精简说明版本**: 1.0
**创建日期**: 2025-01-29
