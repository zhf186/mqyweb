# 漫骑游项目 - 立即开始部署

## 📋 当前状态

✅ 所有部署文件已创建完成
✅ 项目代码已准备就绪
⏳ 准备推送到 GitHub 并部署到服务器

---

## 🎯 部署步骤（请按顺序执行）

### 步骤 1：提交代码到 GitHub

#### 方式一：使用 Git GUI 工具（推荐）

如果您使用 GitHub Desktop 或其他 Git GUI 工具：

1. 打开 Git 客户端
2. 查看所有新增和修改的文件
3. 提交消息：`准备部署到阿里云 - 添加完整部署方案`
4. 推送到 GitHub 仓库 `git@github.com:zhf186/mqyweb.git`

#### 方式二：使用命令行（如果已安装 Git）

```bash
# 在项目根目录执行
git add .
git commit -m "准备部署到阿里云 - 添加完整部署方案"
git push origin main
```

**新增的文件清单**：
- docker-compose.prod.yml
- .env.production
- backend/manqiyou-app/Dockerfile
- frontend/Dockerfile
- deploy/nginx-manqiyou.conf
- deploy/quick-deploy.sh
- deploy/setup-ssl.sh
- deploy/deploy-to-aliyun.sh
- deploy/阿里云部署完整方案.md
- deploy/README-ALIYUN.md
- deploy/DEPLOYMENT_CHECKLIST.md
- deploy/部署文件说明.md
- PROJECT_SUMMARY.md
- DEPLOYMENT_READY.md
- 开始部署-操作指南.md（本文件）

---

### 步骤 2：连接到阿里云服务器

使用 SSH 客户端连接到服务器：

**服务器信息**：
- IP: 47.97.21.33
- 用户: root（或您的用户名）
- 端口: 22

#### Windows 用户

**方式一：使用 PowerShell**
```powershell
ssh root@47.97.21.33
```

**方式二：使用 PuTTY**
1. 打开 PuTTY
2. Host Name: 47.97.21.33
3. Port: 22
4. 点击 Open
5. 输入用户名和密码

**方式三：使用 Windows Terminal**
```bash
ssh root@47.97.21.33
```

---

### 步骤 3：在服务器上配置 Git SSH 密钥

连接到服务器后，执行以下命令：

```bash
# 1. 生成 SSH 密钥（如果还没有）
ssh-keygen -t ed25519 -C "your-email@example.com"
# 按 Enter 使用默认路径
# 按 Enter 跳过密码（或设置密码）

# 2. 查看公钥
cat ~/.ssh/id_ed25519.pub

# 3. 复制显示的公钥内容
```

**然后在 GitHub 上添加 SSH 密钥**：
1. 访问 https://github.com/settings/keys
2. 点击 "New SSH key"
3. Title: "阿里云服务器"
4. Key: 粘贴刚才复制的公钥
5. 点击 "Add SSH key"

**测试连接**：
```bash
ssh -T git@github.com
# 应该看到：Hi zhf186! You've successfully authenticated...
```

---

### 步骤 4：克隆项目代码

```bash
# 1. 创建项目目录
sudo mkdir -p /var/www/manqiyou
sudo chown -R $USER:$USER /var/www/manqiyou

# 2. 克隆代码
cd /var/www/manqiyou
git clone git@github.com:zhf186/mqyweb.git .

# 3. 确认文件已下载
ls -la
```

**预期结果**：应该看到项目的所有文件和文件夹

---

### 步骤 5：配置环境变量

```bash
cd /var/www/manqiyou

# 1. 复制环境变量模板
cp .env.production .env

# 2. 编辑环境变量
nano .env
```

**修改以下内容**（使用强密码）：

```bash
# 数据库密码（请使用强密码，至少 16 位）
DB_PASSWORD=Manqiyou2024!@#$%^&*()

# Redis 密码（请使用强密码）
REDIS_PASSWORD=Redis2024!@#$%^&*()

# JWT 密钥（使用随机字符串）
JWT_SECRET=your_jwt_secret_change_this_to_random_string_32_chars

# API URL（已配置好，无需修改）
NEXT_PUBLIC_API_URL=https://www.zjmqy.cc/api
```

**保存并退出**：
- 按 `Ctrl + X`
- 按 `Y` 确认
- 按 `Enter` 保存

---

### 步骤 6：运行一键部署脚本

```bash
cd /var/www/manqiyou

# 添加执行权限
chmod +x deploy/quick-deploy.sh

# 运行部署脚本
bash deploy/quick-deploy.sh
```

**这个脚本会自动**：
1. 检查环境依赖
2. 停止旧容器（如果存在）
3. 构建 Docker 镜像（需要 10-15 分钟）
4. 启动所有容器
5. 验证服务状态

**等待构建完成**，您会看到类似的输出：
```
✓ 环境检查通过
✓ 项目目录已创建
✓ 代码已更新
✓ 环境变量配置已就绪
✓ 旧容器已停止
✓ Docker 容器已启动
✓ 服务启动等待完成
✓ 服务状态检查完成
```

---

### 步骤 7：配置 Nginx 反向代理

```bash
# 1. 复制 Nginx 配置文件
sudo cp /var/www/manqiyou/deploy/nginx-manqiyou.conf /etc/nginx/sites-available/manqiyou

# 2. 创建软链接启用站点
sudo ln -s /etc/nginx/sites-available/manqiyou /etc/nginx/sites-enabled/

# 3. 测试 Nginx 配置
sudo nginx -t

# 4. 如果测试通过，重载 Nginx
sudo systemctl reload nginx

# 5. 检查 Nginx 状态
sudo systemctl status nginx
```

**预期结果**：
- `nginx -t` 显示 "syntax is ok" 和 "test is successful"
- Nginx 服务状态为 "active (running)"

---

### 步骤 8：配置 SSL 证书（HTTPS）

```bash
# 1. 编辑 SSL 配置脚本，修改邮箱地址
sudo nano /var/www/manqiyou/deploy/setup-ssl.sh

# 找到这一行：
# EMAIL="your-email@example.com"
# 改为你的实际邮箱，例如：
# EMAIL="admin@zjmqy.cc"

# 保存并退出（Ctrl+X, Y, Enter）

# 2. 添加执行权限
sudo chmod +x /var/www/manqiyou/deploy/setup-ssl.sh

# 3. 运行 SSL 配置脚本
sudo bash /var/www/manqiyou/deploy/setup-ssl.sh
```

**脚本会自动**：
1. 安装 Certbot
2. 获取 Let's Encrypt SSL 证书
3. 配置自动续期
4. 测试续期功能

**按照提示操作**：
- 确认邮箱地址
- 同意服务条款
- 等待证书获取完成

---

### 步骤 9：验证部署

#### 9.1 测试 HTTPS 访问

```bash
# 测试 HTTPS
curl -I https://www.zjmqy.cc

# 应该返回 200 OK
```

#### 9.2 在浏览器中访问

打开浏览器，访问：
- https://www.zjmqy.cc

**检查项**：
- [ ] 页面正常加载
- [ ] 浏览器显示安全锁图标
- [ ] 图片正常显示
- [ ] 所有页面可访问
- [ ] 中英文切换正常

#### 9.3 测试 API

```bash
# 测试后端 API
curl https://www.zjmqy.cc/api/health

# 应该返回 {"status":"ok"} 或类似内容
```

---

## ✅ 部署完成检查清单

使用 `deploy/DEPLOYMENT_CHECKLIST.md` 进行完整检查：

```bash
# 在服务器上查看检查清单
cat /var/www/manqiyou/deploy/DEPLOYMENT_CHECKLIST.md
```

### 核心检查项

- [ ] 所有 Docker 容器都在运行
- [ ] 网站可通过 HTTPS 访问
- [ ] HTTP 自动跳转到 HTTPS
- [ ] SSL 证书有效
- [ ] 所有页面正常加载
- [ ] API 接口正常工作
- [ ] 图片和静态资源正常显示
- [ ] 自动续期已配置

---

## 🔧 常用管理命令

### 查看服务状态

```bash
cd /var/www/manqiyou

# 查看所有容器
docker-compose -f docker-compose.prod.yml ps

# 查看日志
docker-compose -f docker-compose.prod.yml logs -f

# 查看特定服务日志
docker-compose -f docker-compose.prod.yml logs -f frontend
docker-compose -f docker-compose.prod.yml logs -f backend
```

### 重启服务

```bash
cd /var/www/manqiyou

# 重启所有服务
docker-compose -f docker-compose.prod.yml restart

# 重启特定服务
docker-compose -f docker-compose.prod.yml restart frontend
docker-compose -f docker-compose.prod.yml restart backend
```

### 更新代码

```bash
cd /var/www/manqiyou

# 拉取最新代码
git pull origin main

# 重新构建并启动
docker-compose -f docker-compose.prod.yml up -d --build
```

### 查看 Nginx 日志

```bash
# 访问日志
sudo tail -f /var/log/nginx/manqiyou_access.log

# 错误日志
sudo tail -f /var/log/nginx/manqiyou_error.log
```

---

## 🐛 常见问题

### 问题 1：容器无法启动

```bash
# 查看详细日志
docker-compose -f docker-compose.prod.yml logs

# 检查端口占用
sudo netstat -tulpn | grep -E ':(3000|8080|5432|6379)'

# 重启 Docker
sudo systemctl restart docker
```

### 问题 2：Nginx 502 错误

```bash
# 检查后端是否运行
curl http://localhost:8080/api/health

# 查看 Nginx 错误日志
sudo tail -f /var/log/nginx/manqiyou_error.log

# 检查容器状态
docker-compose -f docker-compose.prod.yml ps
```

### 问题 3：SSL 证书获取失败

```bash
# 检查域名解析
nslookup www.zjmqy.cc

# 检查 80 端口是否可访问
curl http://www.zjmqy.cc

# 查看 Certbot 日志
sudo journalctl -u certbot -n 50
```

---

## 📞 获取帮助

### 详细文档

- **完整部署指南**: `deploy/阿里云部署完整方案.md`
- **检查清单**: `deploy/DEPLOYMENT_CHECKLIST.md`
- **快速参考**: `deploy/README-ALIYUN.md`

### 查看日志

```bash
# Docker 容器日志
docker-compose -f docker-compose.prod.yml logs -f

# Nginx 日志
sudo tail -f /var/log/nginx/manqiyou_error.log

# 系统日志
sudo journalctl -xe
```

---

## 🎉 部署成功！

当所有检查项都通过后，您的网站就成功部署了！

**访问地址**: https://www.zjmqy.cc

**恭喜！漫骑游项目已成功部署到阿里云！** 🚀

---

**操作指南版本**: 1.0
**创建日期**: 2025-01-29
