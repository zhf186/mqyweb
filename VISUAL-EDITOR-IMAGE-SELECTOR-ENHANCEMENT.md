# 可视化编辑器 - 图片选择器增强功能

**创建时间**: 2026-02-14  
**状态**: 📋 规划中  
**优先级**: 高

## 功能概述

增强可视化编辑器的图片编辑功能，添加图片选择器，让用户可以从图片库中直接选择图片进行替换，而不需要手动输入图片路径。

## 用户需求

> "在后台管理的可视化编辑，点击图片后，会跳出该图片显示，并有该页面所有其他图片的显示，可以进行选择某张图片进行替换，并能自动适配大小。"

## 功能特性

### 1. 图片库集成
- 从图片管理系统获取该页面的所有可用图片
- 根据页面分类（home、ebike、routes等）自动筛选
- 显示图片缩略图、文件名、尺寸信息

### 2. 可视化选择
- 4列网格布局显示图片缩略图
- 点击图片即可选中
- 选中状态高亮显示（蓝色边框 + 勾选图标）
- 显示图片详细信息（尺寸、大小）

### 3. 便捷操作
- 选中图片后自动填充路径到输入框
- 保留手动输入路径的功能
- 实时预览新图片
- 一键保存替换

### 4. 自动适配
- 图片更新后自动适配容器大小
- 使用响应式CSS确保不同尺寸图片正常显示
- 保持页面布局不变形

## 技术实现

### 前端组件更新

**文件**: `frontend/src/components/admin/visual-editor/ImageEditDialog.tsx`

#### 主要改动

1. **添加图片列表查询**
```typescript
const { data: assetsResponse, isLoading } = useQuery({
  queryKey: ['assets', pageSlug],
  queryFn: () => assetApi.getAssets({ category: pageSlug }),
  enabled: isOpen,
})
```

2. **图片网格显示**
```typescript
<div className="grid grid-cols-4 gap-3 max-h-[400px] overflow-y-auto">
  {assets.map((asset) => (
    <div
      key={asset.id}
      className={cn(
        'relative border-2 rounded-lg cursor-pointer',
        selectedImage?.id === asset.id && 'border-blue-500 ring-2'
      )}
      onClick={() => handleSelectImage(asset)}
    >
      <img src={asset.thumbnailUrl} alt={asset.originalFilename} />
      {selectedImage?.id === asset.id && <CheckCircle />}
    </div>
  ))}
</div>
```

3. **图片选择处理**
```typescript
const handleSelectImage = (asset: Asset) => {
  setSelectedImage(asset)
  setImagePath(asset.fileUrl)
}
```

### 组件Props更新

```typescript
interface ImageEditDialogProps {
  element: EditableElement
  pageSlug: string  // 新增：用于筛选该页面的图片
  isOpen: boolean
  onClose: () => void
  onSave: (imagePath: string) => Promise<void>
}
```

### UI布局

```
┌─────────────────────────────────────────────────────────┐
│  编辑图片                                          [X]   │
├─────────────────────────────────────────────────────────┤
│                                                         │
│  当前图片                                               │
│  ┌───────────────────────────────────────────────────┐ │
│  │                                                   │ │
│  │         [当前图片预览]                             │ │
│  │                                                   │ │
│  └───────────────────────────────────────────────────┘ │
│                                                         │
│  选择图片                                               │
│  ┌───────────────────────────────────────────────────┐ │
│  │ ┌────┐ ┌────┐ ┌────┐ ┌────┐                      │ │
│  │ │图1 │ │图2 │ │图3 │ │图4 │                      │ │
│  │ └────┘ └────┘ └────┘ └────┘                      │ │
│  │ ┌────┐ ┌────┐ ┌────┐ ┌────┐                      │ │
│  │ │图5 │ │图6 │ │图7 │ │图8 │                      │ │
│  │ └────┘ └────┘ └────┘ └────┘                      │ │
│  └───────────────────────────────────────────────────┘ │
│                                                         │
│  图片路径（或手动输入）                                  │
│  ┌───────────────────────────────────────────────────┐ │
│  │ /brand_assets/home/hero.jpg                       │ │
│  └───────────────────────────────────────────────────┘ │
│                                                         │
│  新图片预览                                             │
│  ┌───────────────────────────────────────────────────┐ │
│  │                                                   │ │
│  │         [新图片预览]                               │ │
│  │                                                   │ │
│  └───────────────────────────────────────────────────┘ │
│                                                         │
│  图片信息                                               │
│  文件名: hero.jpg                                       │
│  尺寸: 1920 × 1080px                                   │
│  大小: 245.67 KB                                       │
│                                                         │
│                                    [取消]  [保存]       │
└─────────────────────────────────────────────────────────┘
```

## 实施任务

### 任务 6.5: 增强图片编辑 - 添加图片选择器

- [ ] 6.5.1 集成图片管理API
  - 在ImageEditDialog中调用assetApi.getAssets
  - 根据pageSlug筛选该页面的图片
  - 实现图片列表加载状态

- [ ] 6.5.2 实现图片网格显示
  - 创建4列网格布局显示图片缩略图
  - 显示图片文件名
  - 实现图片选中状态高亮
  - 添加选中图标（CheckCircle）

- [ ] 6.5.3 实现图片选择交互
  - 点击图片缩略图选中该图片
  - 自动填充图片路径到输入框
  - 显示选中图片的详细信息（尺寸、大小）

- [ ] 6.5.4 处理空状态和错误
  - 显示"暂无图片"提示
  - 提供"前往图片管理上传"链接
  - 处理图片加载失败

- [ ] 6.5.5 优化弹窗布局
  - 扩大弹窗尺寸（max-w-4xl）
  - 添加滚动区域（max-h-[400px]）
  - 优化响应式布局

- [ ] 6.5.6 实现图片自动适配
  - 确保前端页面图片使用响应式CSS类
  - 验证图片更新后自动适配容器
  - 测试不同尺寸图片的显示效果

## 验收标准

### 功能验收

1. ✅ 点击图片元素后，弹窗显示当前图片
2. ✅ 弹窗显示该页面所有可用图片的缩略图网格
3. ✅ 点击某张图片缩略图，该图片被选中并高亮
4. ✅ 选中图片后，图片路径自动填充到输入框
5. ✅ 显示选中图片的详细信息（文件名、尺寸、大小）
6. ✅ 保存后，新图片在预览页面正确显示
7. ✅ 新图片自动适配容器大小，不变形
8. ✅ 如果该页面没有图片，显示"暂无图片"提示
9. ✅ 提供"前往图片管理上传"链接

### 性能验收

1. ✅ 图片列表加载时间 < 1秒
2. ✅ 图片选择响应时间 < 100ms
3. ✅ 弹窗打开时间 < 300ms
4. ✅ 图片网格滚动流畅

### 用户体验验收

1. ✅ 图片缩略图清晰可见
2. ✅ 选中状态明显易识别
3. ✅ 操作流程简单直观
4. ✅ 错误提示友好明确

## 测试计划

### 单元测试

- [ ] 测试图片列表API调用
- [ ] 测试图片选择逻辑
- [ ] 测试路径自动填充
- [ ] 测试空状态处理

### 集成测试

- [ ] 测试完整的图片选择流程
- [ ] 测试图片保存和更新
- [ ] 测试不同页面的图片筛选
- [ ] 测试图片自动适配

### 手动测试

1. 进入可视化编辑器
2. 点击首页的Hero背景图
3. 验证弹窗显示首页的所有图片
4. 选择一张新图片
5. 验证路径自动填充
6. 保存并验证图片更新
7. 验证图片自动适配容器大小

## 相关文件

### 需要修改的文件

- `frontend/src/components/admin/visual-editor/ImageEditDialog.tsx` - 主要修改
- `frontend/src/components/admin/visual-editor/VisualEditor.tsx` - 传递pageSlug参数
- `.kiro/specs/visual-page-editor/requirements.md` - 已更新
- `.kiro/specs/visual-page-editor/design.md` - 已更新
- `.kiro/specs/visual-page-editor/tasks.md` - 已更新

### 依赖的API

- `assetApi.getAssets({ category: pageSlug })` - 获取图片列表
- `contentApi.updateContentItem()` - 保存图片路径

### 依赖的组件

- `Dialog` - shadcn/ui对话框
- `Button` - shadcn/ui按钮
- `Input` - shadcn/ui输入框
- `Loader2` - lucide-react加载图标
- `CheckCircle` - lucide-react勾选图标
- `ImageIcon` - lucide-react图片图标
- `AlertCircle` - lucide-react警告图标

## 后续优化建议

1. **图片搜索**: 添加图片搜索功能，快速找到需要的图片
2. **图片上传**: 在弹窗中直接上传新图片
3. **图片裁剪**: 提供简单的图片裁剪功能
4. **图片预设**: 为不同位置推荐合适尺寸的图片
5. **最近使用**: 显示最近使用的图片，方便快速选择
6. **收藏功能**: 收藏常用图片，提高效率

## 注意事项

1. **图片分类**: 确保图片管理系统的分类与页面slug一致
2. **缩略图**: 使用thumbnailUrl而不是fileUrl以提高加载速度
3. **响应式**: 确保弹窗在不同屏幕尺寸下正常显示
4. **错误处理**: 妥善处理图片加载失败的情况
5. **性能**: 大量图片时考虑分页或虚拟滚动

---

**文档创建**: 2026-02-14  
**预计工作量**: 1-2天  
**优先级**: 高  
**状态**: 待实施
