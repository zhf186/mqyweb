# MySQL 数据库迁移完成报告

## 迁移概述

项目已成功从 H2 内存数据库迁移到 MySQL 持久化数据库。

**迁移日期**: 2026-02-10  
**迁移状态**: ✅ 完成（待测试）

## 已完成的工作

### 1. 后端配置修改

#### ✅ Maven 依赖更新 (`pom.xml`)
- 添加 MySQL Connector/J 依赖
- 将 H2 和 PostgreSQL 设置为可选依赖
- 保持向后兼容性

#### ✅ 数据源配置更新 (`application.yml`)
- 修改数据源为 MySQL
- 配置环境变量支持
- 配置 HikariCP 连接池
- 更新 SQL 初始化配置指向 `schema-mysql.sql`

#### ✅ MySQL 架构文件 (`schema-mysql.sql`)
- 转换所有表定义为 MySQL 兼容语法
- 修改自增主键：`IDENTITY` → `AUTO_INCREMENT`
- 修改布尔类型：`BOOLEAN` → `TINYINT(1)`
- 添加表引擎和字符集：`ENGINE=InnoDB DEFAULT CHARSET=utf8mb4`
- 优化时间戳字段：添加 `ON UPDATE CURRENT_TIMESTAMP`
- 内联索引定义

**转换的表（共 20+ 张）：**
- CMS 系统：`cms_admins`, `cms_routes`, `cms_products`, `cms_partners`, `cms_assets`, `cms_contents`, `cms_operation_logs`, `cms_system_settings`
- 用户系统：`users`, `user_profiles`
- 业务数据：`routes`, `categories`, `orders`, `order_items`, `members`, `member_benefits`
- 其他：`verification_codes`, `refresh_tokens`, `goods`, `partners`

### 2. 配置文件创建

#### ✅ 环境变量模板 (`.env.example`)
```properties
DB_HOST=localhost
DB_PORT=3306
DB_NAME=manqiyou
DB_USERNAME=root
DB_PASSWORD=your_password_here
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=
OSS_ACCESS_KEY_ID=your-access-key-id
OSS_ACCESS_KEY_SECRET=your-access-key-secret
OSS_BUCKET=manqiyou-cms
OSS_ENDPOINT=oss-cn-hangzhou.aliyuncs.com
OSS_CUSTOM_DOMAIN=
```

### 3. 文档创建

#### ✅ MySQL 配置指南 (`MYSQL-SETUP.md`)
- MySQL 安装指南（Windows/Linux/macOS）
- 数据库创建步骤
- 环境变量配置
- 常见问题解答
- 性能优化建议
- 生产环境部署建议

#### ✅ 迁移指南 (`H2-TO-MYSQL-MIGRATION.md`)
- 迁移原因说明
- 详细的技术变更说明
- 用户操作步骤
- 数据迁移说明
- 回退方案
- 性能对比
- 技术细节

#### ✅ 测试脚本 (`test-mysql-schema.bat`)
- 自动检查 MySQL 安装
- 创建数据库
- 导入架构和数据
- 验证表结构
- 统计记录数

#### ✅ 更新主文档 (`README.md`)
- 添加 MySQL 安装指南
- 更新快速启动步骤
- 添加数据库配置说明
- 更新访问地址信息
- 添加 MySQL 相关常见问题

## 文件清单

### 修改的文件
- `backend/manqiyou-app/pom.xml`
- `backend/manqiyou-app/src/main/resources/application.yml`
- `README.md`

### 新增的文件
- `backend/manqiyou-app/.env.example`
- `backend/manqiyou-app/src/main/resources/schema-mysql.sql`
- `backend/manqiyou-app/MYSQL-SETUP.md`
- `backend/manqiyou-app/H2-TO-MYSQL-MIGRATION.md`
- `backend/manqiyou-app/test-mysql-schema.bat`
- `MYSQL-MIGRATION-COMPLETE.md` (本文件)

### 保留的文件（向后兼容）
- `backend/manqiyou-app/src/main/resources/schema.sql` (H2 版本)
- `backend/manqiyou-app/src/main/resources/data.sql` (数据初始化)

## 用户操作指南

### 快速开始（5 步）

#### 1. 安装 MySQL
```bash
# Windows - 使用 Chocolatey
choco install mysql

# 或下载官方安装包
# https://dev.mysql.com/downloads/mysql/
```

#### 2. 创建数据库
```bash
mysql -u root -p
```
```sql
CREATE DATABASE manqiyou CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
EXIT;
```

#### 3. 配置环境变量
```bash
cd backend/manqiyou-app
copy .env.example .env
# 编辑 .env 文件，填入 MySQL 密码
```

#### 4. 停止当前后端服务
如果后端正在运行（进程 ID: 4），先停止：
```bash
# 在 Kiro 中停止进程，或按 Ctrl+C
```

#### 5. 启动应用
```bash
cd backend/manqiyou-app
mvnw.cmd spring-boot:run
```

### 验证迁移

1. **检查应用启动日志**
   - 确认连接到 MySQL
   - 确认表创建成功

2. **访问 API**
   ```bash
   curl http://localhost:8080/api/health
   ```

3. **登录管理后台**
   - 访问 http://localhost:3000/admin/login
   - 用户名：`admin`
   - 密码：`Admin@123`

4. **检查数据**
   - 路线管理：应显示 6 条示例路线
   - 商品管理：应显示 4 个示例商品
   - 合作伙伴：应显示 4 个示例合作伙伴

## 技术变更详情

### 数据库连接

**之前（H2）：**
```yaml
spring:
  datasource:
    url: jdbc:h2:mem:manqiyou
    driver-class-name: org.h2.Driver
    username: sa
    password:
```

**现在（MySQL）：**
```yaml
spring:
  datasource:
    url: jdbc:mysql://${DB_HOST:localhost}:${DB_PORT:3306}/${DB_NAME:manqiyou}?useUnicode=true&characterEncoding=utf8&useSSL=false&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: ${DB_USERNAME:root}
    password: ${DB_PASSWORD:}
```

### SQL 语法变更

| 特性 | H2 | MySQL |
|------|-----|-------|
| 自增主键 | `GENERATED BY DEFAULT AS IDENTITY` | `AUTO_INCREMENT` |
| 布尔类型 | `BOOLEAN` | `TINYINT(1)` |
| 时间戳 | `TIMESTAMP DEFAULT CURRENT_TIMESTAMP` | `TIMESTAMP DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP` |
| 表引擎 | 无 | `ENGINE=InnoDB` |
| 字符集 | 无 | `DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci` |
| 索引 | 分离定义 | 内联定义 |

### 连接池配置

新增 HikariCP 连接池配置：
```yaml
spring:
  datasource:
    hikari:
      minimum-idle: 5
      maximum-pool-size: 20
      idle-timeout: 600000
      max-lifetime: 1800000
      connection-timeout: 30000
```

## 优势对比

| 特性 | H2 内存数据库 | MySQL |
|------|--------------|-------|
| 数据持久化 | ❌ 重启丢失 | ✅ 持久保存 |
| 生产就绪 | ❌ 仅开发用 | ✅ 生产级 |
| 并发性能 | 中等 | 优秀 |
| 查询优化 | 基础 | 高级（索引、查询计划） |
| 备份恢复 | ❌ 不支持 | ✅ mysqldump/binlog |
| 主从复制 | ❌ 不支持 | ✅ 支持 |
| 监控工具 | 有限 | 丰富（慢查询、性能监控） |
| 云服务支持 | ❌ 无 | ✅ RDS/Aurora 等 |

## 兼容性说明

### 代码兼容性
- ✅ **无需修改 Java 代码**：MyBatis-Plus 自动适配不同数据库
- ✅ **无需修改 Entity**：注解保持不变
- ✅ **无需修改 Mapper**：SQL 语法由 MyBatis-Plus 处理

### 数据兼容性
- ✅ **初始数据保持一致**：`data.sql` 无需修改
- ✅ **管理员账号不变**：用户名 `admin`，密码 `Admin@123`
- ✅ **示例数据完整**：路线、商品、合作伙伴数据完整迁移

### 向后兼容性
- ✅ **可回退到 H2**：保留 `schema.sql` 和 H2 依赖
- ✅ **配置切换简单**：修改 `application.yml` 即可切换

## 测试建议

### 功能测试
- [ ] 管理员登录
- [ ] 路线管理（增删改查）
- [ ] 商品管理（增删改查）
- [ ] 合作伙伴管理（增删改查）
- [ ] 资产上传
- [ ] 内容管理
- [ ] 操作日志查看
- [ ] 系统设置

### 性能测试
- [ ] 并发登录测试
- [ ] 大量数据查询测试
- [ ] 分页性能测试
- [ ] 图片上传性能测试

### 压力测试
- [ ] 100 并发用户
- [ ] 1000 条数据查询
- [ ] 长时间运行稳定性

## 后续优化建议

### 短期（1-2 周）
1. **索引优化**
   - 分析慢查询日志
   - 添加复合索引

2. **缓存策略**
   - 配置 Redis 缓存
   - 缓存热点数据

3. **监控配置**
   - 配置慢查询日志
   - 设置连接数告警

### 中期（1-2 月）
1. **读写分离**
   - 配置主从复制
   - 读操作分散到从库

2. **分区表**
   - 对日志表按时间分区
   - 定期归档历史数据

3. **备份策略**
   - 配置自动备份
   - 测试恢复流程

### 长期（3-6 月）
1. **云数据库迁移**
   - 评估阿里云 RDS
   - 规划迁移方案

2. **高可用架构**
   - 多主复制
   - 自动故障转移

3. **性能调优**
   - 查询优化
   - 表结构优化

## 常见问题

### Q: 迁移后原有数据会丢失吗？
A: H2 是内存数据库，本身就不保存数据。迁移到 MySQL 后，数据会持久化保存。

### Q: 需要修改代码吗？
A: 不需要。MyBatis-Plus 会自动适配不同数据库。

### Q: 迁移需要多长时间？
A: 约 10-15 分钟，包括安装 MySQL、配置和验证。

### Q: 可以回退到 H2 吗？
A: 可以。修改 `application.yml` 配置即可切换回 H2。

### Q: 生产环境怎么部署？
A: 建议使用云数据库服务（如阿里云 RDS），无需手动安装 MySQL。

## 相关文档

- 📖 [MySQL 配置指南](backend/manqiyou-app/MYSQL-SETUP.md)
- 📖 [迁移详细指南](backend/manqiyou-app/H2-TO-MYSQL-MIGRATION.md)
- 📖 [项目 README](README.md)
- 🔧 [测试脚本](backend/manqiyou-app/test-mysql-schema.bat)

## 技术支持

如遇到问题，请：
1. 查看应用日志
2. 检查 MySQL 服务状态
3. 验证 `.env` 配置
4. 参考常见问题文档

---

**迁移完成！** 🎉

下一步：安装 MySQL 并测试应用启动。
