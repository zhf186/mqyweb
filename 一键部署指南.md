# 一键部署指南

## 🚀 最简单的部署方法

### 方法一：使用本地 BAT 脚本（推荐）

1. **确保代码已推送**
   ```powershell
   cd D:\mrcweb1
   git push origin main
   ```

2. **运行部署脚本**
   ```powershell
   .\deploy-to-server.bat
   ```

3. **按照提示操作**
   - 输入服务器密码（2-3次）
   - 首次部署需要配置 GitHub SSH 密钥
   - 等待完成（15-20分钟）

---

### 方法二：直接在服务器上执行（更简单）

如果方法一遇到问题，可以直接在服务器上执行：

#### 步骤 1：连接到服务器

```powershell
ssh root@47.97.21.33
```

#### 步骤 2：下载并执行部署脚本

```bash
# 下载部署脚本
curl -o server-deploy.sh https://raw.githubusercontent.com/zhf186/mqyweb/main/deploy/server-deploy.sh

# 或者如果上面的命令失败，使用 wget
wget https://raw.githubusercontent.com/zhf186/mqyweb/main/deploy/server-deploy.sh

# 添加执行权限
chmod +x server-deploy.sh

# 执行部署
bash server-deploy.sh
```

#### 步骤 3：按照提示操作

脚本会自动完成所有部署步骤，您只需要：
1. 首次部署时，将显示的 SSH 公钥添加到 GitHub
2. 如果需要配置 SSL，输入您的邮箱地址
3. 等待部署完成

---

### 方法三：手动复制脚本到服务器

如果无法直接下载脚本：

#### 步骤 1：连接到服务器

```powershell
ssh root@47.97.21.33
```

#### 步骤 2：创建部署脚本

```bash
# 创建脚本文件
nano server-deploy.sh
```

#### 步骤 3：复制脚本内容

从项目中的 `deploy/server-deploy.sh` 文件复制所有内容，粘贴到编辑器中。

保存：`Ctrl+X`, `Y`, `Enter`

#### 步骤 4：执行部署

```bash
chmod +x server-deploy.sh
bash server-deploy.sh
```

---

## 📋 部署脚本功能

自动部署脚本会完成：

1. ✅ **环境检查** - 检查 Docker、Git、Nginx
2. ✅ **SSH 配置** - 配置 GitHub SSH 密钥
3. ✅ **代码获取** - 克隆或更新代码
4. ✅ **环境配置** - 配置环境变量
5. ✅ **Docker 部署** - 构建并启动容器
6. ✅ **Nginx 配置** - 配置反向代理
7. ✅ **SSL 配置** - 配置 HTTPS 证书
8. ✅ **验证部署** - 测试服务状态

---

## ⚠️ 首次部署注意事项

### 1. GitHub SSH 密钥

首次运行时，脚本会显示：

```
==========================================
重要: 请将以下公钥添加到 GitHub
访问: https://github.com/settings/keys
==========================================
ssh-ed25519 AAAAC3NzaC1lZDI1NTE5AAAA... deploy@zjmqy.cc
==========================================

添加完成后按 Enter 继续...
```

**操作**：
1. 复制显示的公钥
2. 访问 https://github.com/settings/keys
3. 点击 "New SSH key"
4. 粘贴公钥并保存
5. 返回终端按 Enter

### 2. SSL 证书配置

如果是首次配置 SSL，脚本会询问：

```
输入您的邮箱地址: 
```

输入您的真实邮箱地址，用于接收证书到期提醒。

---

## 🔍 部署进度说明

### 预计时间：15-20 分钟

| 步骤 | 说明 | 时间 |
|------|------|------|
| [1/8] | 检查环境 | 10秒 |
| [2/8] | 配置 SSH 密钥 | 30秒 |
| [3/8] | 创建项目目录 | 10秒 |
| [4/8] | 克隆代码 | 1-2分钟 |
| [5/8] | 配置环境变量 | 10秒 |
| [6/8] | 构建 Docker 容器 | **10-15分钟** ⏰ |
| [7/8] | 配置 Nginx | 30秒 |
| [8/8] | 配置 SSL | 1-2分钟 |

**最耗时的是步骤 6**，需要下载依赖和构建镜像。

---

## ✅ 部署成功标志

看到以下输出表示部署成功：

```
==========================================
部署完成！
==========================================

访问地址: https://www.zjmqy.cc

常用命令:
  查看日志: cd /var/www/manqiyou && docker-compose -f docker-compose.prod.yml logs -f
  重启服务: cd /var/www/manqiyou && docker-compose -f docker-compose.prod.yml restart
  停止服务: cd /var/www/manqiyou && docker-compose -f docker-compose.prod.yml down
```

---

## 🔧 部署后验证

### 1. 在浏览器中访问

```
https://www.zjmqy.cc
```

检查：
- ✅ 页面正常加载
- ✅ 浏览器显示安全锁图标
- ✅ 图片正常显示
- ✅ 中英文切换正常

### 2. 测试所有页面

- https://www.zjmqy.cc/about
- https://www.zjmqy.cc/routes
- https://www.zjmqy.cc/ebike
- https://www.zjmqy.cc/community
- https://www.zjmqy.cc/partners
- https://www.zjmqy.cc/goods

### 3. 在服务器上检查

```bash
# 查看容器状态
cd /var/www/manqiyou
docker-compose -f docker-compose.prod.yml ps

# 查看日志
docker-compose -f docker-compose.prod.yml logs -f

# 测试后端 API
curl http://localhost:8080/api/health
```

---

## 🔄 更新部署

代码更新后，重新部署：

### 方法 1：在本地推送后自动部署

```powershell
# 1. 推送新代码
cd D:\mrcweb1
git add .
git commit -m "更新说明"
git push origin main

# 2. 重新运行部署脚本
.\deploy-to-server.bat
```

### 方法 2：在服务器上手动更新

```bash
# 连接服务器
ssh root@47.97.21.33

# 更新代码
cd /var/www/manqiyou
git pull origin main

# 重新构建并启动
docker-compose -f docker-compose.prod.yml up -d --build
```

---

## 🐛 常见问题

### Q1: SSH 连接失败

```powershell
# 测试连接
ssh root@47.97.21.33

# 如果失败，检查：
# 1. 服务器 IP 是否正确
# 2. 服务器是否在线
# 3. 防火墙设置
```

### Q2: GitHub 克隆失败

```bash
# 测试 GitHub 连接
ssh -T git@github.com

# 如果失败，重新配置 SSH 密钥
ssh-keygen -t ed25519 -C "deploy@zjmqy.cc"
cat ~/.ssh/id_ed25519.pub
# 添加到 GitHub
```

### Q3: Docker 构建失败

```bash
# 查看详细日志
docker-compose -f docker-compose.prod.yml logs

# 重启 Docker
sudo systemctl restart docker

# 重新构建
docker-compose -f docker-compose.prod.yml up -d --build
```

### Q4: 网站无法访问

```bash
# 检查容器状态
docker-compose -f docker-compose.prod.yml ps

# 检查 Nginx 状态
sudo systemctl status nginx

# 查看 Nginx 日志
sudo tail -f /var/log/nginx/manqiyou_error.log

# 测试本地访问
curl http://localhost:3000
curl http://localhost:8080/api/health
```

### Q5: SSL 证书问题

```bash
# 查看证书状态
sudo certbot certificates

# 重新获取证书
sudo certbot delete --cert-name www.zjmqy.cc
sudo bash /var/www/manqiyou/deploy/setup-ssl.sh
```

---

## 📞 获取帮助

### 查看日志

```bash
# Docker 日志
cd /var/www/manqiyou
docker-compose -f docker-compose.prod.yml logs -f

# 前端日志
docker-compose -f docker-compose.prod.yml logs -f frontend

# 后端日志
docker-compose -f docker-compose.prod.yml logs -f backend

# Nginx 日志
sudo tail -f /var/log/nginx/manqiyou_access.log
sudo tail -f /var/log/nginx/manqiyou_error.log
```

### 重启服务

```bash
cd /var/www/manqiyou

# 重启所有服务
docker-compose -f docker-compose.prod.yml restart

# 重启特定服务
docker-compose -f docker-compose.prod.yml restart frontend
docker-compose -f docker-compose.prod.yml restart backend
```

---

## 🎯 快速开始（推荐流程）

```powershell
# === 在本地 Windows 上 ===

# 1. 确保代码已推送
cd D:\mrcweb1
git push origin main

# 2. 运行自动部署
.\deploy-to-server.bat

# === 或者直接在服务器上 ===

# 1. 连接服务器
ssh root@47.97.21.33

# 2. 执行部署脚本
bash <(curl -s https://raw.githubusercontent.com/zhf186/mqyweb/main/deploy/server-deploy.sh)

# === 等待完成 ===
# 15-20 分钟后访问: https://www.zjmqy.cc
```

---

**版本**: 1.0  
**最后更新**: 2025-01-29  
**预计部署时间**: 15-20 分钟  
**难度**: ⭐⭐☆☆☆ (简单)
