# 自动部署到阿里云服务器

## 🚀 一键部署

### 使用方法

1. **确保已推送代码到 GitHub**
   ```powershell
   # 在 D:\mrcweb1 目录
   git push origin main
   ```

2. **运行自动部署脚本**
   ```powershell
   # 双击运行
   deploy-to-server.bat
   
   # 或在 PowerShell 中运行
   .\deploy-to-server.bat
   ```

3. **按照提示操作**
   - 输入服务器密码（会提示 2-3 次）
   - 如果是首次部署，需要配置 GitHub SSH 密钥（脚本会显示公钥）
   - 等待部署完成（约 15-20 分钟）

---

## 📋 脚本功能

自动部署脚本会执行以下操作：

### 1. 环境检查
- ✓ 检查 Docker
- ✓ 检查 Docker Compose
- ✓ 检查 Git
- ✓ 检查 Nginx

### 2. SSH 密钥配置
- ✓ 生成 SSH 密钥（如果不存在）
- ✓ 显示公钥供添加到 GitHub
- ✓ 测试 GitHub 连接

### 3. 代码部署
- ✓ 创建项目目录
- ✓ 克隆或更新代码
- ✓ 配置环境变量

### 4. Docker 部署
- ✓ 构建 Docker 镜像
- ✓ 启动所有容器
- ✓ 验证服务状态

### 5. Nginx 配置
- ✓ 复制 Nginx 配置
- ✓ 测试配置
- ✓ 重载 Nginx

### 6. SSL 配置
- ✓ 检查现有证书
- ✓ 配置新证书（如果需要）

### 7. 验证部署
- ✓ 检查容器状态
- ✓ 测试 HTTPS 访问
- ✓ 显示访问地址

---

## ⚠️ 首次部署注意事项

### 1. GitHub SSH 密钥配置

首次运行时，脚本会生成 SSH 密钥并显示公钥：

```
==========================================
重要: 请将以下公钥添加到 GitHub
访问: https://github.com/settings/keys
==========================================
ssh-ed25519 AAAAC3NzaC1lZDI1NTE5AAAA... deploy@zjmqy.cc
==========================================

添加完成后按 Enter 继续...
```

**操作步骤**：
1. 复制显示的公钥
2. 访问 https://github.com/settings/keys
3. 点击 "New SSH key"
4. Title: "阿里云服务器"
5. Key: 粘贴公钥
6. 点击 "Add SSH key"
7. 返回终端，按 Enter 继续

### 2. SSL 证书配置

首次配置 SSL 时，需要：
1. 确认邮箱地址
2. 同意服务条款
3. 等待证书获取完成

脚本会自动处理这些步骤。

---

## 🔧 手动部署（备选方案）

如果自动脚本遇到问题，可以手动执行：

### 1. 连接服务器
```powershell
ssh root@47.97.21.33
```

### 2. 执行部署命令
```bash
# 配置 SSH 密钥
ssh-keygen -t ed25519 -C "deploy@zjmqy.cc"
cat ~/.ssh/id_ed25519.pub
# 添加到 GitHub: https://github.com/settings/keys

# 克隆代码
sudo mkdir -p /var/www/manqiyou
sudo chown -R $USER:$USER /var/www/manqiyou
cd /var/www/manqiyou
git clone git@github.com:zhf186/mqyweb.git .

# 配置环境变量
cp .env.production .env

# 部署
chmod +x deploy/quick-deploy.sh
bash deploy/quick-deploy.sh

# 配置 Nginx
sudo cp deploy/nginx-manqiyou.conf /etc/nginx/sites-available/manqiyou
sudo ln -s /etc/nginx/sites-available/manqiyou /etc/nginx/sites-enabled/
sudo nginx -t
sudo systemctl reload nginx

# 配置 SSL
sudo chmod +x deploy/setup-ssl.sh
sudo bash deploy/setup-ssl.sh
```

---

## 📊 部署时间估算

| 步骤 | 预计时间 |
|------|---------|
| 环境检查 | 10 秒 |
| SSH 密钥配置 | 30 秒 |
| 代码克隆 | 1-2 分钟 |
| Docker 构建 | 10-15 分钟 |
| Nginx 配置 | 30 秒 |
| SSL 配置 | 1-2 分钟 |
| 验证部署 | 30 秒 |
| **总计** | **15-20 分钟** |

---

## ✅ 部署成功标志

部署成功后，您会看到：

```
==========================================
部署完成！
==========================================

访问地址: https://www.zjmqy.cc

常用命令:
  查看日志: cd /var/www/manqiyou && docker-compose -f docker-compose.prod.yml logs -f
  重启服务: cd /var/www/manqiyou && docker-compose -f docker-compose.prod.yml restart
  停止服务: cd /var/www/manqiyou && docker-compose -f docker-compose.prod.yml down
```

---

## 🔍 故障排查

### 问题 1: SSH 连接失败

**错误**: `Connection refused` 或 `Permission denied`

**解决**:
```powershell
# 测试 SSH 连接
ssh root@47.97.21.33

# 如果失败，检查：
# 1. 服务器 IP 是否正确
# 2. 服务器是否开启 SSH 服务
# 3. 防火墙是否允许 SSH 连接
```

### 问题 2: GitHub 克隆失败

**错误**: `Permission denied (publickey)`

**解决**:
1. 确保已将服务器的 SSH 公钥添加到 GitHub
2. 测试连接: `ssh -T git@github.com`
3. 如果失败，重新生成密钥并添加到 GitHub

### 问题 3: Docker 构建失败

**错误**: `docker: command not found` 或构建错误

**解决**:
```bash
# 检查 Docker 状态
sudo systemctl status docker

# 重启 Docker
sudo systemctl restart docker

# 查看详细日志
docker-compose -f docker-compose.prod.yml logs
```

### 问题 4: Nginx 配置错误

**错误**: `nginx: configuration file test failed`

**解决**:
```bash
# 测试配置
sudo nginx -t

# 查看错误日志
sudo tail -f /var/log/nginx/error.log

# 检查配置文件
sudo nano /etc/nginx/sites-available/manqiyou
```

### 问题 5: SSL 证书获取失败

**错误**: `Failed to obtain certificate`

**解决**:
1. 确保域名已正确解析到服务器 IP
2. 检查防火墙是否开放 80 和 443 端口
3. 查看 Certbot 日志: `sudo certbot certificates`

---

## 🔄 更新部署

代码更新后，重新运行部署脚本：

```powershell
# 1. 推送新代码
cd D:\mrcweb1
git add .
git commit -m "更新说明"
git push origin main

# 2. 重新部署
.\deploy-to-server.bat
```

脚本会自动：
- 拉取最新代码
- 重新构建镜像
- 重启容器

---

## 📞 获取帮助

如果遇到问题：

1. **查看日志**
   ```bash
   cd /var/www/manqiyou
   docker-compose -f docker-compose.prod.yml logs -f
   ```

2. **检查容器状态**
   ```bash
   docker-compose -f docker-compose.prod.yml ps
   ```

3. **查看 Nginx 日志**
   ```bash
   sudo tail -f /var/log/nginx/manqiyou_error.log
   ```

4. **参考详细文档**
   - `deploy/阿里云部署完整方案.md`
   - `完整部署流程.md`

---

## 🎯 快速开始

```powershell
# 1. 确保代码已推送
cd D:\mrcweb1
git push origin main

# 2. 运行自动部署
.\deploy-to-server.bat

# 3. 等待完成（15-20 分钟）

# 4. 访问网站
# https://www.zjmqy.cc
```

---

**版本**: 1.0  
**最后更新**: 2025-01-29  
**预计部署时间**: 15-20 分钟
